# Program: Sub-list Removal Variants

l = [1, 1, 2, 3, 4]       # example list
length = len(l)
result = []              # to store unique sub-lists

for i in range(length):
    new_list = l[:i] + l[i+1:]     # remove element at index i

    if new_list not in result:     # check for duplicates
        result.append(new_list)
        print(new_list)

print("\nFinal Output:")
print(result)
