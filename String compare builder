str1 = input("Enter a string: ")
l = len(str1)
new_l = ['0'] * l

for i in range(1, l):
    j = i - 1
    for ch in range(j, -1, -1):
        if str1[j] != str1[ch]:
            new_l[i] = str1[ch]
            break

print(new_l)
